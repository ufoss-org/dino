sourceCompatibility = JavaVersion.VERSION_14
targetCompatibility = JavaVersion.VERSION_14

dependencies {
    implementation project(':dino-core')

    testImplementation testFixtures(project(':dino-core'))
}
